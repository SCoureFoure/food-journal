---
name: audit_2026_05_15
description: INV and DIR test gap fill — invariance_test.dart and directional_test.dart created; all 145 tests pass
metadata:
  type: project
---

## [2026-05-15] ai-scout — INV/DIR coverage gap fill

**Gaps addressed:** INV (0 tests → 47 tests), DIR (1 true test → 13 tests)

**New files created:**

- `app/test/meal_memory/invariance_test.dart` — 47 INV tests across 8 groups
- `app/test/meal_memory/directional_test.dart` — 13 DIR tests in 1 group

**INV test groups written:**

- `isReferential` — case (UPPER/Title/mIxEd), trailing punctuation (`.!...???`), prepended punctuation, leading/trailing whitespace, double internal spaces
- `leftovers` — all seed patterns (leftovers/leftover/the rest of/what's left/what was left), case invariant
- `yesterday/last_night` — yesterday, last night, the night before, the other night; case + punctuation variants
- `named_day` — monday/friday case invariant, punctuation on named day
- `same_as_before` — again, the usual, repeat, same old, my go-to, the thing I had; case + punctuation
- `days_ago` — a few days ago, last week, a while back, couple days back; case + punctuation
- `this_morning` — this morning, earlier today, a few hours ago; case + punctuation
- `synonym seeds` — all seeds for leftovers/same_as_before/yesterday/days_ago/this_morning produce identical output

**DIR test contracts written:**

1. Vague → specific: `had that again` (matchRecent=true, offset=null) → `had that again on monday` (matchRecent=false, offset=3)
2. Vague → specific generalized: `the usual` → `the usual on tuesday`
3. Vague dateOffset=null → specific dateOffset set: `repeat dinner` → `repeat dinner last friday`
4. Meal type additive baseline: `leftovers yesterday` has no mealType, offset=1
5. Meal type additive: `leftovers from dinner yesterday` adds mealType without removing offset
6. Meal type additive + days_ago: offset unchanged when breakfast keyword added
7. Meal type additive + named_day: lunch keyword does not disturb Wednesday offset
8. Confidence additive: more rules → higher totalConfidence
9. Duplicate temporal → higher confidence than single
10. Named day overrides leftovers default: plain leftovers=1, leftovers+friday=6
11. Named day overrides yesterday: both present → named_day wins (offset=3 not 1)
12. Priority: two_days_ago > yesterday — "day before yesterday" = offset 2 not 1
13. Priority: days_ago > this_morning — "earlier this week" = offset 3 not 0

**Test count:** 98 (prior) → 145 total (+47 INV, +13 DIR; prior 4-file total was 98)

**All tests passed on first run.** No rule changes required — the existing engine already satisfies every invariant (case-insensitive RegExp flags were already set; normalize+trim already handled in detectReferences).

**Still open (carried from prior audit):**

- No integration test for rolling window pruning (requires real DB)
- No widget test for AI-off path (requires mock AiService)
- No test for Worker behavior with context block (requires network mock)
- Worker `foods` array guard (Flutter client would throw if object returned)

**Patterns confirmed by INV sweep:**

- `caseSensitive: false` on every RegExp in meal_reference_rules.dart is doing the right work — no rules need patching for case invariance
- `text.trim().toLowerCase()` at the top of `detectReferences()` handles all leading/trailing/double-space variants except internal double-space; internal double-space doesn't matter because `\b` word boundary and `\s+` patterns absorb extra whitespace naturally
- synonym seeds for `this_morning` include `\bearlier\b` which fires on `earlier this week` too — days_ago priority override is confirmed working by DIR test 13
