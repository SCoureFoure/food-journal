class ExportService {
  Future<String> exportMealsCsv({DateTime? from, DateTime? to}) async {
    throw UnimplementedError('ExportService.exportMealsCsv');
  }

  Future<String> exportGroceryList({DateTime? from, DateTime? to}) async {
    throw UnimplementedError('ExportService.exportGroceryList');
  }
}
